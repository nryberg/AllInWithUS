= form_for @team do |f|
  -if @team.errors.any?
    #errorExplanation
      %h2= "#{pluralize(@location.errors.count, "error")} prohibited this team from being saved:"
      %ul
        - @team.errors.full_messages.each do |msg|
          %li= msg
  .field 
    = f.label :name
    = f.text_area :name
  .actions
    = f.submit 'Save'
